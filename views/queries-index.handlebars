<div class="row">
    <div class="col-sm-8 col-sm-offset-2">
        <ul>
            {{#each queries}}
                <li class="list-group-item">
                    <div class="lead">
                        <a href="/queries/{{this._id}}">Season {{this.season}}, Round {{this.round}}</a>
                    </div>
                </li>
            {{/each}}
        </ul>
    </div>
</div>